extends layout

block content
  h1.page-title Pet Store Inventory

  div.wrapper
    div.category-container
      a.newitem-link(href='/newItem') Add New item
      each category in categories
        a.category-title(href='/category/'+category)=category
